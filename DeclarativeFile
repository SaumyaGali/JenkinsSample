pipeline {
    agent any
    stages {
        stage('Check Git') {
            steps { 
                git 
            }
        stage('Build Maven') {
            steps { 
                sh 'mvn clean package'
            }
        }
    }
}
