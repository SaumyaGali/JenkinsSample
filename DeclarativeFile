pipeline {
    agent any
    stages {
        stage('Check Git') {
            steps { 
                git 'https://github.com/SaumyaGali/JenkinsSample.git'
            }
            }
        stage('Build Maven') {
            steps { 
                sh 'mvn clean package'
            }
        }
    }
}
